# UpcoreAgent Server — Environment Variables
# Copy to .env and fill in values

# Claude API key — get from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Login password — share this string with developers who need access
# Choose something strong (e.g., 3 random words + numbers: BluePanda927!)
AGENT_PASSWORD=<your-strong-password>

# JWT signing secret — signs the token returned after login
# Generate with: openssl rand -hex 32
AGENT_JWT_SECRET=<generate-with-openssl-rand-hex-32>

# Server port (Railway sets this automatically in production)
PORT=3000

NODE_ENV=development
